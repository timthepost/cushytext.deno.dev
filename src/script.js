/**
 * Here for site customization, plugins that need to write things,
 * or whatever else is needed to make things work. This is loaded
 * last by the template, and is here so you don't have to edit the
 * template scripts directly.
 */
